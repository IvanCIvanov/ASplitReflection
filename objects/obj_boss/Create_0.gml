// Attacks at specific intervals

// Include his movement

// Larger health bar
// draw_healthbar(x1, y1, x2, y2, hp, c_black, c_red, c_lime, 0, true, true);

// Melee swipe attack for when player is close

// Ranged snowball attack for when player is far away

attack_interval = 200;	// smaller attack interval at half health
is_active = false;	// becomes active once all henchman are defeated
can_attack = false;		// determines whether or not the boss can attack